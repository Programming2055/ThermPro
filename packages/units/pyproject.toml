[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "thermpro-units"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = []

[tool.hatch.build.targets.wheel]
packages = ["src/thermpro_units"]

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.mypy]
strict = true
