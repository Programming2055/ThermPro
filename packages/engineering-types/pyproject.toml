[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "thermpro-engineering-types"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "thermpro-engine",
]

[tool.hatch.build.targets.wheel]
packages = ["src/thermpro_engineering_types"]
